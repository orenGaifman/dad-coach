package com.dadcoach.ai.prompt;

/**
 * Enumeration of all versioned prompt types in the system.
 * Each prompt type corresponds to a distinct AI function/template category.
 */
public enum PromptType {
    SYSTEM,
    PERSONA,
    MISSION_GEN,
    REFLECTION,
    SUMMARY,
    ONBOARDING,
    CELEBRATION,
    FOLLOW_UP,
    INACTIVITY,
    SAFETY
}
